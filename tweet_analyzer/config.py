
"""set up the twitter api secret keys"""

import streamlit as st

st.secrets["CONSUMER_KEY"])
st.secrets["CONSUMER_SECRET"])
st.secrets["ACCESS_TOKEN"])
st.secrets["ACCESS_TOKEN_SECRET"])
st.secrets["BEARER_TOKEN"])

